We will use the `air quality` data set that is distributed in
the `datasets` package which is part of the default
installation of R. This data set is immediately accessible from your R
session and can be investigated using 

